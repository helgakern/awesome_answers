<nav class='navbar navbar-expand-lg navbar-light bg-light'>
  <ul class='navbar-nav mr-auto'>
    <%# link_to 'Question Index', '/questions' %>
    <%= link_to 'Question Index', questions_path %>
    |
    <%= link_to 'Question New Page', new_question_path %>
  </ul>
  <div>
    <% if user_signed_in? %>
      <%# this button created by link_to should DELETE /session %>
      <%= link_to "Logout", session_path, method: :delete, class: 'btn btn-danger' %>
      <%= current_user.first_name %> <%= current_user.last_name %>
    <% else %>
      <%= link_to "Log In", new_session_path, class: 'btn btn-success'%>
    <% end %>
  </div>
</nav>